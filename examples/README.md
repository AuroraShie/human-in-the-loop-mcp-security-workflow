# Synthetic example

`example-session.json` is a fictional, closed session for `example.com`. It demonstrates the persisted state shape after authorization, asset confirmation, five interface records, one hypothesis, a Candidate lead, manual replay, a Confirmed result, and closure.

It does not represent a real target, request, security finding, or exploit payload.
